{
  "name": "@queryquest/admin",
  "version": "1.0.0",
  "description": "QueryQuest Admin Panel (static)",
  "private": true,
  "scripts": {
    "build": "echo 'Admin is a static site — no build step required'",
    "dev": "echo 'Serve index.html with any static server'"
  }
}
